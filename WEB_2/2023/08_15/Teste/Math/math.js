const PI = 3.1415;

const soma = (a,b) => {
    return a + b;
}

const subtrai = (a,b) => {
    return a - b;
}

const pow = (b,e)=>{
    return b^e;
}

const math = {
    PI: PI,
    soma: soma,
    subtrai : subtrai,
    pow: pow
}

export default math;